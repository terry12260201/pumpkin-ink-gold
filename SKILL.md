---
name: pumpkin-ink-gold
description: >-
  南瓜墨金美學（Pumpkin Ink & Gold）。使用者指定「墨金、Ink & Gold、pumpkin-ink-gold」或要求沿用此設計系統時使用。
  用於網站、工具目錄、知識平台、網頁工具、簡報與文件的視覺設計。提供互動淺灰點陣、墨色文字、克制金色、白色橫卡與中文排版規格。
---

# 南瓜墨金美學 · Ink & Gold 1.2

**淡紙承載內容，墨字建立順序，金色指出下一步。**
先決定讀者要「找東西、了解產品，還是聽你說明」，再選版型；顏色相同，不代表每一頁骨架相同。

## 先選用途

| 用途 | 版型 | 標題與節奏 |
|---|---|---|
| 工具目錄、知識庫、資源平台 | `ig-directory` | 置中短導讀 → 分類與搜尋 → 三欄橫卡；標題 36–40px/600，手機 30px |
| 品牌介紹、產品首頁 | 原有品牌元件 | 可用 62px/900 與 300 粗細反差、插圖與深色舞台；依內容選用 |
| 操作工具、儀表板 | 工作區 | 先露出輸入、結果與狀態；標題 24–32px；不塞宣傳 Hero |
| 簡報、文件 | 敘事頁 | 一頁一件事；依輸出尺寸換算字級，見應用食譜 |

## 不變的視覺規則

- 紙：`#F5F5F5` 互動點陣；卡：`#FFFFFF`；墨：`#161415`。游標靠近時點的位置內縮，按鈕邊緣有輪廓吸附，離開回復；不可用靜態紋理當作完整互動版本。
- 金：`#FDC302 → #FFD83A`，用在主要動作。相鄰區域避免重複搶眼主按鈕；金色底配深炭字。
- 卡：18px 圓角，1px 墨色 8% 邊線；預設幾乎無陰影。目錄 hover 上移 1px、200ms。
- 目錄：**內容淨寬 1200px、欄距 12px、卡內距 16px、圖文間距 16px、Logo 76px**。
- 中文：Noto Sans TC / 蘋方，正體、字距 0；標題 600，內文 400。Roboto 負責英文。中英不硬套同一行數。
- 工具卡標題 17px；描述 14px，中文行高 1.6–1.75。長中文標題可留兩行，整排高度一致。
- 圖示：一般操作用單色線條圖示。**內容辨識圖與工具 Logo 一律照 `references/圖示風格.md`**（macOS 風圓角方塊、同色相上亮下暗漸層、單一白色主體佔 60%、微立體、不描邊；8 組底色，墨 ≤2、金 ≤1）。品牌標誌保留比例與留白。
- 頁面是否需要插圖、深色舞台、等寬眉標，取決於內容；目錄沒有這些也成立。
- 重要小字用可讀墨色 `--ig-label-readable`；45% 墨色是原站次要色，不直接當作所有小字的無障礙保證。

規範定稿：2026-09-18。範例用來示範，不把範例內容變成每個專案的固定需求。

## 開工時讀哪裡

1. 色彩、字級：[色彩與字體](references/色彩與字體.md)。
2. 精準目錄尺寸、觀察與改編界線：[design.md](design.md)。
3. 元件組合：[元件與版面](references/元件與版面.md)。
4. 其他媒介：[應用食譜](references/應用食譜.md)。
5. 給 AI 的完整指令：[AI咒語](references/AI咒語.md)。
6. 實作套件：[ink-gold.css](references/ink-gold.css)，可內嵌或連結；中文網頁設 `lang="zh-Hant"`。

目錄頁在 body 加 `class="ig ig-directory"`。品牌頁只用 `ig`。既有程式保留資料、路由、功能與品牌資產，改視覺層。
完整範例在 `assets/demo-directory.html`；卡片是示範資料，點擊開說明，不代表已接上工具。
互動與主題引入 `references/ink-gold-ui.js`；總覽／目錄有切換按鈕，既有平台沿用自己的主題控制。品牌素材與圖示來源見 `assets/brand/`、`assets/icons/`。
改 CSS 後跑 `python3 scripts/inline_kit.py`，讓總覽與兩個範例一起更新。

## 完成條件

- 同一畫面能分清標題、摘要與狀態；圖片不搶過內容。
- 1440、768、414、375、320px 沒有橫向溢出、被截斷的操作或壓扁 Logo。
- 搜尋、分類、清空結果、卡片目的地、鍵盤焦點可用；頁面保留真實資料，示意資料明確標示。
- 實際移動滑鼠檢查空白處收攏、按鈕邊緣吸附、離開回復；不能只看靜態截圖。
- 檢查深淺色切換、重整記憶與減少動態設定；若聲稱離線可用，要實際用 file:// 開啟測試。
- 用實際畫面對照 `design.md`，記錄測過哪些尺寸與功能。不要把主觀分數當測試通過證據。

## 素材與適用範圍

這是可重用的設計語言，不包含特定平台的開發排程或私有資料。參考頁的觀察來源記在 `design.md`；南瓜 Logo 使用 `assets/brand/` 既有檔案。網站第三方品牌商標、照片與程式碼並未隨本套件授權。

## v1.2 定案補充（2026-09-20 合併）
- **互動點格＝Pumpkin Notes 定案版是預設**（`ink-gold-ui.js` 的 `glow` 模式：點不動、靠近變亮變大，24px 點距）。南瓜指定「一定要保留、不能改壞」。磁吸模式只在明確要求時用 `data-dots="magnetic"`。
- 公司品牌、導覽列只放 Logo、產品名稱規則、已落地的卡片目錄與報告頁規格，見 `references/互動點格與品牌.md`；做任何墨金網站先讀它。
