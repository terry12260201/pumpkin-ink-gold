---
name: pumpkin-ink-gold
description: >-
  🖋️ 南瓜墨金美學（Pumpkin Ink & Gold）——乾淨、專業、有編輯感的「知識平台／工具目錄」設計語言：
  淺灰點格紙底、近黑墨色文字、金色只畫重點、白卡髮絲線、深炭色舞台收尾、粗黑＋細體標題反差。
  當使用者提到「墨金、墨金美學、Ink & Gold、ink-gold、黑金風、白底金色、極簡知識平台風、工具目錄風、
  卡片目錄、一張一張卡片、乾淨專業的網站、知識庫網站、目錄頁、提案簡報要乾淨專業」或點名要用這套風格
  做網站／網頁工具／平台／中控台／儀表板／簡報／文件／社群圖卡／電子報／XR 介面時，觸發此 skill。
  內含：CSS 套件（直接貼上可用）、色彩與字體（含中文排版規則）、元件與版面規格、各種應用食譜、
  給其他 AI 的咒語、驗收清單。
  ⚠️ 分工：要「手繪水彩、遊戲感、溫暖有人味」→ 改用 `pumpkin-cozy-isle`（南瓜暖島美學）；
  使用者只說「照我的風格」而沒點名時，先問要墨金還是暖島。
---

# 🖋️ 南瓜墨金美學 Pumpkin Ink & Gold

> **一句話定義**：像一本排版講究的筆記本——**墨色寫字、點格打底、金色只畫重點**。
> **要讓人覺得**：清楚、可信、被好好整理過、專業但不冷。
> **不要讓人覺得**：花俏、科技霓虹、AI 罐頭模板、企業後台的灰暗。

---

## 🧠 大腦（檔案）位置

| 要做什麼 | 讀哪個檔 |
|---|---|
| 直接開工寫網頁 | `references/ink-gold.css`（整段貼進 `<style>`，`<body class="ig">`） |
| 查色碼、字級、中文排版 | `references/色彩與字體.md` |
| 查元件尺寸、狀態、版面骨架、圖示／Logo／插圖／動態 | `references/元件與版面.md` |
| 做簡報、文件、社群圖、電子報、XR 介面、圖表 | `references/應用食譜.md` |
| 要叫別的 AI（GPT／Gemini／生圖）照這風格做 | `references/AI咒語.md` |
| 看實際長相 | `index.html`（設計總覽）、`assets/demo-directory.html`（工具目錄頁）、`assets/demo-slides.html`（簡報） |
| 改了 CSS 套件後同步到所有 HTML | `python3 scripts/inline_kit.py` |

線上版：https://terry12260201.github.io/pumpkin-ink-gold/

---

## 🧬 七個基因（每一條都要做到才像）

| # | 基因 | 規則 | 常見錯誤 |
|---|---|---|---|
| 1 | **紙** 點格底 | 頁面底 `#F5F5F5` ＋ 16px 間距淡點（`--ig-dot`） | 用純白底、用漸層背景 |
| 2 | **墨** 一色分層 | 文字只用 `#161415`，靠透明度分層：100 標題／65 內文／45 標籤／18 hover 框／8 髮絲線 | 換成好幾種灰、用純黑 `#000` |
| 3 | **金** 只畫重點 | `#FDC302→#FFD83A` 漸層；**一個畫面只有一顆金色主按鈕**；金色按鈕上的字用深炭 `#2D2B2C` | 金色到處用、金底配白字 |
| 4 | **字** 粗細反差 | 大標＝「900 粗黑一行＋300 細體一行」；眉標與數字用等寬字 Roboto Mono | 整頁都 600、沒有細體 |
| 5 | **卡** 白卡髮絲線 | 白卡＋1px 8% 墨線＋**18px 圓角**；平常沒陰影，hover 才浮起 2px | 厚陰影、粗邊框、圓角不一 |
| 6 | **炭** 深色舞台 | 重點段落用 `#2D2B2C→#242223` 深炭大圓角（32px）區塊收尾；**一頁最多 2 塊** | 整頁深色、深色區塊太多 |
| 7 | **動** 輕快 | 120／200／320ms，`cubic-bezier(.4,0,.2,1)`；只做浮起、變色、淡入 | 彈跳、旋轉、長動畫 |

---

## 🎨 色彩速查（完整版見 `references/色彩與字體.md`）

| Token | 色碼 | 用在哪 |
|---|---|---|
| `--ig-paper` | `#F5F5F5` | 頁面底（加點格） |
| `--ig-card` | `#FFFFFF` | 卡片、輸入框、未選膠囊 |
| `--ig-ink` | `#161415` | 標題、選中膠囊底、主要文字 |
| `--ig-ink-muted` | `#161415` 65% | 內文、描述 |
| `--ig-ink-soft` | `#161415` 45% | 標籤、時間、計數 |
| `--ig-ink-hairline` | `#161415` 8% | 卡片邊框、分隔線 |
| `--ig-night` | `#2D2B2C` | 導覽列、深色舞台、墨色按鈕 |
| `--ig-night-deep` | `#242223` | 深色卡片 |
| `--ig-gold` | `#FDC302` | 金色按鈕、焦點、第一順位資料 |
| `--ig-gold-light` | `#FFD83A` | 金色漸層上端 |
| `--ig-gold-soft` | `#FCD94A` | 焦點光圈、深底 hover 字 |
| `--ig-gold-deep` | `#B88800` | 淺底上的金色文字（`#FDC302` 在白底上讀不清） |
| `--ig-mark-yellow／pink／blue` | `#FDE68A／#FDA4AF／#BAE6FD` | 螢光筆重點、插圖點綴（少量） |
| `--ig-success／danger` | `#059669／#DC2626` | ✓／✗ 對比清單、狀態 |
| `--ig-chart-1…5` | `#FDC302 #4DA770 #D97858 #4D6BFE #615CED` | 圖表系列色（依序使用） |

---

## 🔤 字體（⚠️ 中文三條鐵則）

```css
--ig-font-sans: "Roboto", "Noto Sans TC", "PingFang TC", "Microsoft JhengHei", -apple-system, "Segoe UI", sans-serif;
--ig-font-mono: "Roboto Mono", "SF Mono", Menlo, Consolas, "Noto Sans TC", monospace;
```

1. **中文不斜體**。瀏覽器會把中文「硬拗斜」，很醜。粗細反差照做（900／300），斜體只給英文數字（包 `<em>`）；中文要強調用**螢光筆底** `.ig-mark`。
2. **中文不負字距**。英文大標 `-0.02~-0.03em`，中文一律 `0`（套件已用 `html[lang^="zh"]` 自動處理，**頁面務必寫 `lang="zh-Hant"`**）。
3. **中文行高加大**。內文 1.75、大標 1.2（英文是 1.5／1.1）。

- **上線網頁**：載 Google Fonts（Roboto 300–900＋900 Italic、Roboto Mono 400/500、Noto Sans TC 300–900）。
- **離線單檔小工具**（南瓜鐵則：file:// 雙擊直開、零外部資源）：**不載 Google Fonts**，靠字體堆疊退到蘋方／正黑。⚠️ 蘋方最粗只有 600（Semibold），所以離線時大標反差改成 **600 對 200**。
- 字級速查：大標 62／h1 40／h2 36／卡片標題 17／內文 16／描述 14／膠囊 14／標籤 12／眉標 12 等寬大寫＋字距 .14em。**中文最小 12px**。

---

## 🧩 元件速查（class 都在 `ink-gold.css`）

| 元件 | class | 關鍵規格 |
|---|---|---|
| 浮動導覽列 | `.ig-nav` `.ig-nav__link.is-active` | 深炭膠囊、高 60、離頂 16、陰影 float |
| 按鈕 | `.ig-btn--gold／--ink／--ghost／--line` ＋ `--sm／--lg` | 膠囊、高 44（sm 32／lg 52）、hover 上浮 1px |
| 文字連結 | `.ig-link` | 底線 hover 變金 |
| 篩選膠囊 | `.ig-pills` `.ig-pill.is-active` `.ig-pill__n` | 高 32、選中＝墨底白字、右側淡色計數 |
| 主題晶片 | `.ig-chip` | 等寬字＋小圖示 |
| 標籤／徽章 | `.ig-tag` `.ig-kicker` `.ig-badge(--gold)` `.ig-live` | 標籤 12px 淡底；區段小徽章白底細框 |
| 卡片 | `.ig-card` `.ig-card--link` | 白底、1px 8% 線、18px、hover 浮起 2px＋邊框 18% |
| 工具卡 | `.ig-tool` `__logo` `__title` `__desc` `__foot` `__arrow` | 左 72px Logo 方塊（12px 圓角）＋標題 17/600＋描述兩行截斷＋右上 ↗ |
| 數據卡 | `.ig-stat` `__n` `__l` | 圖示＋等寬大數字＋小說明 |
| 便當格 | `.ig-tile` `__art` `__body` `--dark` | 上方 180px 淡格線插圖區＋下方文字 |
| 搜尋 | `.ig-search` `.ig-kbd` | 高 52、14px 圓角、focus 金色光圈 |
| 輸入框 | `.ig-input` | 高 44、12px 圓角；在 `.ig-stage` 內自動變深色 |
| 進度條 | `.ig-progress(--ink／--blue) > i` | 高 6、圓頭 |
| 深色舞台 | `.ig-stage` `.ig-stage-card` | 深炭漸層、32px 圓角、大陰影 |
| 清單 | `.ig-checks`（金勾）`.ig-compare .no／.yes`（紅叉灰字／綠勾黑字） | |
| FAQ | `.ig-acc details summary` | 放在 `.ig-stage` 內 |
| 版面 | `.ig-container(--wide／--narrow)` `.ig-section` `.ig-grid(--2／--4)` | 1100／1280／720；區段上下 96（手機 64）；卡片間距 14 |
| 字級 | `.ig-display(__strong／__light)` `.ig-h1` `.ig-h2` `.ig-h3` `.ig-lead` `.ig-body` `.ig-small` `.ig-meta` `.ig-eyebrow` `.ig-num` `.ig-mark` | |
| 夜間版 | `<body class="ig" data-theme="night">` | 中控台、長時間盯螢幕、XR 介面 |

---

## 🧭 圖示、Logo、插圖（完整版見 `references/元件與版面.md`）

- **圖示**：線條圖示（Lucide 系，ISC 授權可商用），`stroke-width: 2`（大於 24px 用 1.75），圓角端點，單色；預設 45% 墨色，hover 變 100%；深底上 70% 白。**不用彩色 emoji 當 UI 圖示**。
- **Logo 方塊**：品牌 Logo 放進白底 12px 圓角方塊（`.ig-tool__logo`），Logo 本身可保留原色——這是全頁唯一允許多彩的地方。
- **自家 Logo 規則**：①符號簡單到 16px 認得出 ②金色符號放深炭底，或墨色符號放紙底 ③英文字標可用 900 斜體、中文字標用 700–900 正體 ④不加立體、不加外發光。
- **插圖**：**單色墨線手繪角色**（白填色、黑輪廓、帶一點塗鴉感）＋**漂浮的彩色圖示方塊**（微傾 ±5–12°、柔和立體、上緣白色高光）＋**金色細連線**。照片少用，要用就放進 18px 圓角容器。

---

## 🚀 開工流程

1. 確認這次要的是墨金（乾淨專業）不是暖島（手繪遊戲感）。
2. 複製 `references/ink-gold.css` 全段進 `<style>`；`<html lang="zh-Hant">`、`<body class="ig">`。
3. 照用途翻 `references/應用食譜.md` 選版面骨架。
4. 只用套件 class 和 `--ig-*` 變數組頁面，**不要自己發明新顏色**。
5. 做完用下方驗收表自評；截 1440 寬全頁圖逐段檢查；純前端小工具要用 `file://` 雙擊直開驗證。

---

## ✅ 驗收清單（加權 ≥ 90 才交付）

| 維度 | 權重 | 看什麼 |
|---|---:|---|
| 金色紀律 | 20% | 每個畫面只有一個金色主角；金底字是深炭；淺底金字用 `#B88800` |
| 墨色層級 | 15% | 只有一個墨色＋透明度；沒有純黑、沒有雜灰 |
| 字體反差 | 15% | 大標有粗細反差；眉標／數字是等寬字；**中文沒有斜體、沒有負字距** |
| 卡片一致 | 15% | 圓角 18、1px 8% 線、無厚陰影、hover 浮起 |
| 版面節奏 | 15% | 容器寬度一致、區段間距 96、卡片間距 14、對齊整齊 |
| 深色舞台 | 10% | 用在收尾或重點、一頁 ≤ 2 塊、內部文字層級正確 |
| 可讀性 | 10% | 1440 寬截圖每個字讀得出來；中文 ≥ 12px；深底文字 ≥ 45% 白 |

## 🚫 一出現就算沒做好

- ❌ 紫藍漸層、霓虹光暈、玻璃擬態、滿版深色科技風
- ❌ 金色按鈕超過一顆、金底白字、白底上直接用 `#FDC302` 當文字
- ❌ 中文斜體、中文負字距、中文掉進新細明體
- ❌ 卡片厚陰影、圓角大小不一、邊框用實心灰線
- ❌ 彩色 emoji 當 UI 圖示、圖示線條粗細混用
- ❌ 假數據直接上線（範例數字要標「示意」）

---

*風格擁有者：陳南宏（南瓜）｜南瓜虛擬科技｜v1.0（2026-09-17）*
